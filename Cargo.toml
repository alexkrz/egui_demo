[package]
name = "egui_demo"
version = "0.1.0"
edition = "2024"

[dependencies]
egui = "0.35"
eframe = "0.35"
# For image support:
egui_extras = { version = "0.35", features = ["default", "image"] }

[profile.release]
opt-level = 2 # fast and small wasm
